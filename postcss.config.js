/* ============================================================
   文件：postcss.config.js
   用途：PostCSS 配置 —— 处理 Tailwind CSS
   维护：通常不需要修改
   ============================================================ */

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
